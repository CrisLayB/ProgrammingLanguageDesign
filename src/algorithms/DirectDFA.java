package algorithms;

import models.State;
import models.Types;
// import models.BTreePrinter;
import models.DFA;
// import models.Symbol;
// import models.Tree;
// import models.SyntaxTree;
import models.PairData;
import models.Tree;

import java.util.ArrayList;

public class DirectDFA {
    public static DFA regularExpressionToDFA(Tree regexTree){
        // Obtener 
        
        // Empezar a construir el automata
        DFA dfa = new DFA(new State(0, Types.Initial));
        
        return dfa;
    }
}
